<% if params[:controller] == 'posts' && params[:action] == 'index' || params[:action] == 'show' %>
	<div class="list-group">
	  <% @categories.each do |category| %>
	  	<%= link_to category.name, category_path(category.id), class: "list-group-item" %>
	  <% end %>
	</div>
<% end %>